import React, { FC } from "react";
import Card from "@/components/card-daisyUi";

const LandingPage: FC = () => {
  return (
    <div>
      <h2>This is the landing page...</h2>
      <Card />
    </div>
  );
};

export default LandingPage;
